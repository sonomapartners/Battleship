(function(global) {
    // ships = [2, 3, 3, 4, 5]
    function setupBoard(ships) {
        // TODO: implement
    }

    function fire(myMoves, theirMoves) {
        // TODO: implement
    }

    global.MyBot = {
        setupBoard: setupBoard,
        fire: fire
    };
})(window);