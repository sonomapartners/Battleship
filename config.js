(function (exports) {
    'use strict';

    exports.CONFIG = {
        // Number of milliseconds between moves being rendered in the UI
        RENDER_DELAY: 20
    };

}(typeof exports !== 'undefined' && exports !== null ? exports : this));